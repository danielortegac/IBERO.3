{
  "entities": {
    "EmailContact": {
      "title": "Email Contact",
      "description": "A saved email address for quick access.",
      "type": "object",
      "properties": {
        "email": { "type": "string", "format": "email" },
        "name": { "type": "string" },
        "addedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["email", "addedAt"]
    },
    "EmailList": {
      "title": "Email List",
      "description": "A named group of email addresses.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "emails": { "type": "array", "items": { "type": "string" } },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "emails", "createdAt"]
    }
  },
  "firestore": {
    "/users/{userId}/mail_contacts/{contactId}": {
      "schema": "EmailContact",
      "description": "Saved individual contacts for Goatify Mail."
    },
    "/users/{userId}/mail_lists/{listId}": {
      "schema": "EmailList",
      "description": "Saved email groups (lists) for Goatify Mail."
    }
  }
}
